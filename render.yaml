services:
  - type: web
    name: unisphere
    env: node
    buildCommand: |
      npm install
      cd client && npm install && npm run build && cd ..
    startCommand: node server/server.js
    envVars:
      - key: NODE_VERSION
        value: 18.x
