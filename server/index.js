











});  console.log(`Server is running on port ${port}`);app.listen(port, () => {app.use('/api/alerts', alertRoutes);const alertRoutes = require('./routes/alerts');app.use(express.json());const port = 5000;const app = express();const express = require('express');